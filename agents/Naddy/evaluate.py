from random import random

from src.Board import Board


def action(state: Board) -> list:
    """Generates possible child states from the given state."""
    # Placeholder: return empty list (no child states)
    return []


def evaluate(state: Board) -> float:
    """Evaluates the given state and returns a reward value."""
    # Placeholder: return rng
    return random()
